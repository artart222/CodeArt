You can run command with arguments in nvim with running this command in terminal:
	nvim -c COMMAND
	example: nvim -c PlugClean

For multiply command you can use syntax like this:
    nvim -c COMMAND1 -c COMMAND2
    Real world example: nvim -c PlugUpdate -c qa!
